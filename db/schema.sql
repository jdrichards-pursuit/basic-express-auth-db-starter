-- db/schema.sql
DROP DATABASE IF EXISTS -- database name
CREATE DATABASE -- database name

\c -- database name

-- you must create a users table, with pertinent info, username, email, password_hash at minimum. password_hash should be a varchar(255) data type


